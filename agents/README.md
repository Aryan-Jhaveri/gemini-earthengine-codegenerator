# Orbital Insight - Multi-Agent Geointelligence Backend

## Setup

```bash
cd agents
python -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

## Environment Variables

Create a `.env` file:
```
GOOGLE_API_KEY=your-gemini-api-key
GOOGLE_CLOUD_PROJECT=your-gcp-project
```
